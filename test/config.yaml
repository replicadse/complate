version: 0.10
templates:
    test:
        content:
            inline: |-
                {{ alpha }}
        values:
            alpha:
                shell: 'printf "alpha"'
    test2:
        content:
            inline: |-
                {{ alpha }}
        values: {}
