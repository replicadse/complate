version: 0.11
templates:
    test:
        content:
            inline: |-
                {{ alpha }}
        values:
            alpha:
                shell: 'printf "alpha"'
    test2:
        content:
            inline: |-
                {{ alpha }}
        values: {}
